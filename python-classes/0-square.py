class Square:
    """A class that defines a square by its size (private)."""

    def __init__(self, size):
        """Initialize a new Square with a private size attribute."""
        self.__size = size
